baseURL = 'https://emrecankoc6.github.io/Literaconite/'
languageCode = 'en-us'
title = 'Literaconite'
theme = 'PaperMod'

[params]
    env = "production"
    defaultTheme = "dark"
    disableThemeToggle = true
    # Ana sayfada poetry ve review klasörlerini listeler
    mainSections = ["poetry", "review"]

[permalinks]
    poetry = "/poetry/:title/"
    review = "/review/:title/"

[[menu.main]]
    name = "Poetry"
    url = "poetry/"
    weight = 10
[[menu.main]]
    name = "Reviews"
    url = "review/"
    weight = 20
